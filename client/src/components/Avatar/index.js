import React, { Component } from 'react';

class Avatar extends Component {

    // <img alt={props.name} src={props.image} />

    render () {
        return (
            <img src="https://via.placeholder.com/150" alt="test-avatar"/>
        )
    }

}

export default Avatar;